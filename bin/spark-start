#!/bin/bash

# this script builds and runs the Spark Docker environment

VERTICA_DEMO_PREFIX=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

cd $VERTICA_DEMO_PREFIX/../docker-spark/docker && docker-compose up -d